# Brik Package Stage
# Container build (conditional - only if package section configured).

brik-package:
  stage: package
  needs:
    - job: brik-test
  script:
    - brik.gitlab.run_stage package
  rules:
    - if: '$CI_COMMIT_TAG'
      when: on_success
    - when: manual
      allow_failure: true
