apiVersion: brik.dev/v1
kind: Provider
metadata:
  id: cosign-kms-openbao
  displayName: Cosign KMS attestation (OpenBAO Transit)
  owner: getbrik
spec:
  capability: artifact-attestation
  binding: referential
  contract: artifact-attestation/v1
  tools:
    - name: cosign
      min_version: "3.0.6"
