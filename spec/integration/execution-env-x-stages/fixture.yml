# L2 fixture manifest -- chantier 20260528_e2e-tests-par-notion (S3, wave 3.1)
edge: execution-env -> stages
graph_edge_id: 1
case: >-
  BRIK_WITH_DEPLOY opt-in must propagate to the deploy stage gate; brik plan
  gates deploy solely on the --with-deploy flag.
synthesized_workspace:
  stack: node
  files: [package.json, brik.yml]
  git: init + single baseline commit
deps_expected: [git, jq, yq]
version_pin:
  brik_schema: plan/v1
  registry: built-in manifest deploy.gate.opt_in_flag = --with-deploy
covers_divergence: D1 (e2e-cross-platform-v0.6.0/REPORT.md)
