apiVersion: brik.dev/v1
kind: Stage
metadata:
  id: init
  displayName: Init
spec:
  module: stages.init
  function: stages.init
  placement: {slot: init, after: [], before: [build]}
  runner: {class: exotic-runner}
  gate: {mode: blocking, contexts: [snapshot, release]}
  api: {required: [stages.init]}
