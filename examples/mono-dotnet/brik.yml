version: 1

project:
  name: my-dotnet-api
  stack: dotnet
  stack_version: "9.0"

build:
  command: dotnet build --configuration Release

test:
  framework: xunit

quality:
  lint:
    tool: dotnet-format
    config: .editorconfig
    fix: false
  format:
    tool: dotnet-format
    check: true
