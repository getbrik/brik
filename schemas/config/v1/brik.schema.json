{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://brik.dev/schemas/config/v1/brik.schema.json",
  "title": "Brik Project Configuration",
  "description": "Declarative project configuration for the Brik portable CI/CD pipeline system. Defines what happens at each stage (stack, tools, thresholds) without describing the pipeline structure itself. The fixed flow Init -> Release -> Build -> Quality/Security -> Test -> Package -> Deploy -> Notify is implemented by platform-specific shared libraries.",
  "type": "object",
  "required": ["version", "project"],
  "additionalProperties": false,
  "properties": {
    "version": {
      "description": "Schema version for brik.yml. Must be 1.",
      "type": "integer",
      "const": 1
    },
    "project": {
      "description": "Project identity and metadata. Required. Provides the name, optional stack hint, and optional monorepo root.",
      "type": "object",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "description": "Project name. Used in logs, notifications, and artefact labels.",
          "type": "string",
          "minLength": 1
        },
        "stack": {
          "description": "Technology stack. Optional - Brik performs auto-detection from project files (package.json -> node, pom.xml -> java, etc.) when omitted.",
          "type": "string",
          "enum": ["node", "java", "python", "dotnet", "rust"]
        },
        "stack_version": {
          "description": "Stack version for runner image selection (e.g. '22' for node, '21' for java). When omitted, uses the default version.",
          "type": "string"
        },
        "root": {
          "description": "Relative path to the service root directory. Used in monorepos where each service has its own brik.yml.",
          "type": "string"
        }
      }
    },
    "release": {
      "description": "Release stage configuration. Absent means semver strategy with 'v' prefix.",
      "$ref": "#/$defs/release"
    },
    "build": {
      "description": "Build stage configuration. Absent means use stack defaults (e.g. npm run build for node, mvn package for java).",
      "$ref": "#/$defs/build"
    },
    "test": {
      "description": "Test stage configuration. Absent means use stack defaults.",
      "$ref": "#/$defs/test"
    },
    "quality": {
      "description": "Quality stage configuration covering lint, formatting, and static analysis. Enabled by default.",
      "$ref": "#/$defs/quality"
    },
    "security": {
      "description": "Security stage configuration covering dependency scanning, secret scanning, and container scanning. Enabled by default.",
      "$ref": "#/$defs/security"
    },
    "package": {
      "description": "Package stage configuration for building and publishing artefacts such as Docker images.",
      "$ref": "#/$defs/package"
    },
    "publish": {
      "description": "Publish stage configuration for publishing artefacts to registries (npm, Docker, Maven, PyPI, Cargo, NuGet).",
      "$ref": "#/$defs/publish"
    },
    "deploy": {
      "description": "Deploy stage configuration. Defines one or more named environments with conditions and deployment targets.",
      "$ref": "#/$defs/deploy"
    },
    "notify": {
      "description": "Notify stage configuration for Slack, email, and webhook notifications.",
      "$ref": "#/$defs/notify"
    },
    "hooks": {
      "description": "Inline pre and post stage hook commands. Evaluated by the shared library (Layer 2). File-based hooks (.brik/hooks/pre-build.sh) are handled separately by the Bash Runtime (Layer 0).",
      "$ref": "#/$defs/hooks"
    }
  },
  "$defs": {
    "release": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "strategy": {
          "description": "Release strategy. Default is semver.",
          "type": "string",
          "enum": ["semver", "calver", "custom"],
          "default": "semver"
        },
        "tag_prefix": {
          "description": "Prefix for release tags (e.g. 'v'). Default is 'v'.",
          "type": "string",
          "default": "v"
        },
        "changelog": {
          "description": "Changelog generation configuration.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "description": "Whether to generate a changelog on release. Default is true.",
              "type": "boolean",
              "default": true
            },
            "format": {
              "description": "Changelog format.",
              "type": "string",
              "enum": ["conventional", "keep-a-changelog"],
              "default": "conventional"
            },
            "file": {
              "description": "Path to the changelog file. Default is CHANGELOG.md.",
              "type": "string",
              "default": "CHANGELOG.md"
            }
          }
        }
      }
    },
    "build": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "command": {
          "description": "Build command to execute. Overrides both tool selection and stack defaults (Tier 1).",
          "type": "string",
          "minLength": 1
        },
        "tool": {
          "description": "Build tool to use (e.g. npm, yarn, pnpm, maven, gradle, poetry, uv, pip, pipenv, cargo, dotnet). Overrides auto-detection from lock/marker files (Tier 2). Ignored when command is set.",
          "type": "string",
          "minLength": 1
        },
        "node_version": {
          "description": "Node.js version to use (e.g. \"20\", \"18\"). Applicable when stack is node.",
          "type": "string",
          "minLength": 1
        },
        "java_version": {
          "description": "Java version to use (e.g. \"21\", \"17\"). Applicable when stack is java.",
          "type": "string",
          "minLength": 1
        },
        "python_version": {
          "description": "Python version to use (e.g. \"3.12\", \"3.11\"). Applicable when stack is python.",
          "type": "string",
          "minLength": 1
        },
        "dotnet_version": {
          "description": ".NET version to use (e.g. \"8.0\", \"9.0\"). Applicable when stack is dotnet.",
          "type": "string",
          "minLength": 1
        },
        "rust_version": {
          "description": "Rust version to use (e.g. \"1.76\", \"stable\"). Applicable when stack is rust.",
          "type": "string",
          "minLength": 1
        }
      }
    },
    "test": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "command": {
          "description": "Test command to execute. Overrides framework-derived and stack-default commands (Tier 1). For per-suite override, use commands.unit/integration/e2e instead.",
          "type": "string",
          "minLength": 1
        },
        "framework": {
          "description": "Test framework to use. Overrides the stack default (e.g. jest for node, junit for java, pytest for python).",
          "type": "string",
          "minLength": 1
        },
        "commands": {
          "description": "Explicit test commands per test type. When present, these override the framework-derived defaults.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "unit": {
              "description": "Command to run unit tests.",
              "type": "string",
              "minLength": 1
            },
            "integration": {
              "description": "Command to run integration tests.",
              "type": "string",
              "minLength": 1
            },
            "e2e": {
              "description": "Command to run end-to-end tests.",
              "type": "string",
              "minLength": 1
            }
          }
        }
      }
    },
    "quality": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Whether the quality stage is active. Set to false to skip lint, format, and SAST checks entirely. Default is true.",
          "type": "boolean",
          "default": true
        },
        "lint": {
          "description": "Linting configuration.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "command": {
              "description": "Lint command to execute. Overrides tool-based and stack-default lint commands (Tier 1).",
              "type": "string",
              "minLength": 1
            },
            "tool": {
              "description": "Lint tool to use (e.g. eslint, biome, oxlint, checkstyle, ruff, flake8, pylint, clippy, dotnet-format). Overrides the stack default (Tier 2).",
              "type": "string",
              "minLength": 1
            },
            "config": {
              "description": "Path to the lint configuration file (e.g. .eslintrc.yml).",
              "type": "string",
              "minLength": 1
            },
            "fix": {
              "description": "Whether to run the linter in auto-fix mode. Default is false.",
              "type": "boolean",
              "default": false
            }
          }
        },
        "format": {
          "description": "Code formatting configuration.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "command": {
              "description": "Format check command to execute. Overrides tool-based and stack-default format commands (Tier 1).",
              "type": "string",
              "minLength": 1
            },
            "tool": {
              "description": "Formatter to use (e.g. prettier, biome, ruff format, black, rustfmt, dotnet-format). Overrides the stack default (Tier 2).",
              "type": "string",
              "minLength": 1
            },
            "check": {
              "description": "Whether to run the formatter in check mode (fail if files would be reformatted) rather than applying changes. Default is false.",
              "type": "boolean",
              "default": false
            }
          }
        },
        "sast": {
          "description": "Static application security testing configuration.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "command": {
              "description": "SAST command to execute. Overrides tool selection (Tier 1).",
              "type": "string",
              "minLength": 1
            },
            "tool": {
              "description": "SAST tool to use (e.g. semgrep, sonarqube, codeql). Overrides auto-detection (Tier 2).",
              "type": "string",
              "minLength": 1
            },
            "ruleset": {
              "description": "Ruleset or profile for the SAST tool (e.g. auto, p/security-audit).",
              "type": "string",
              "minLength": 1
            }
          }
        },
        "deps": {
          "description": "Dependency vulnerability scanning configuration.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "command": {
              "description": "Dependency scan command. Overrides tool-based scanning (Tier 1).",
              "type": "string",
              "minLength": 1
            },
            "severity": {
              "description": "Minimum severity level that causes the dependency scan to fail.",
              "type": "string",
              "enum": ["critical", "high", "medium", "low"]
            },
            "tool": {
              "description": "Dependency scanning tool to use (e.g. npm-audit, pip-audit, osv-scanner).",
              "type": "string",
              "minLength": 1
            }
          }
        },
        "type_check": {
          "description": "Type checking configuration.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "command": {
              "description": "Type check command to execute. Overrides tool selection (Tier 1).",
              "type": "string",
              "minLength": 1
            },
            "tool": {
              "description": "Type checker to use (e.g. tsc, mypy, pyright). Overrides auto-detection (Tier 2).",
              "type": "string",
              "minLength": 1
            }
          }
        },
        "coverage": {
          "description": "Test coverage threshold enforcement.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "threshold": {
              "description": "Minimum coverage percentage required. Default is 80.",
              "type": "integer",
              "minimum": 0,
              "maximum": 100,
              "default": 80
            },
            "report": {
              "description": "Path to the coverage report file (Cobertura XML).",
              "type": "string",
              "minLength": 1
            }
          }
        },
        "license": {
          "description": "License compliance checking configuration.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "allowed": {
              "description": "Comma-separated list of allowed licenses.",
              "type": "string"
            },
            "denied": {
              "description": "Comma-separated list of denied licenses.",
              "type": "string"
            }
          }
        },
        "container": {
          "description": "Container image vulnerability scanning configuration.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "image": {
              "description": "Container image to scan.",
              "type": "string",
              "minLength": 1
            },
            "severity": {
              "description": "Minimum severity level that causes the container scan to fail.",
              "type": "string",
              "enum": ["critical", "high", "medium", "low"]
            }
          }
        }
      }
    },
    "security": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Whether the security stage is active. Set to false to skip all security scans. Default is true.",
          "type": "boolean",
          "default": true
        },
        "dependency_scan": {
          "description": "Whether to scan project dependencies for known vulnerabilities.",
          "type": "boolean"
        },
        "secret_scan": {
          "description": "Whether to scan the codebase for accidentally committed secrets, tokens, or credentials.",
          "type": "boolean"
        },
        "container_scan": {
          "description": "Whether to scan the built Docker image for OS and application vulnerabilities. Typically meaningful only when package.docker is configured.",
          "type": "boolean"
        },
        "dependency_scan_tool": {
          "description": "Tool for dependency vulnerability scanning (e.g. npm-audit, pip-audit, safety, osv-scanner, grype, cargo-audit). Auto-detected from stack when omitted.",
          "type": "string",
          "minLength": 1
        },
        "secret_scan_tool": {
          "description": "Tool for secret scanning (e.g. gitleaks, trufflehog). Defaults to gitleaks if available.",
          "type": "string",
          "minLength": 1
        },
        "container_scan_tool": {
          "description": "Tool for container image scanning (e.g. grype, dockle). Auto-detected when omitted.",
          "type": "string",
          "minLength": 1
        },
        "severity_threshold": {
          "description": "Minimum vulnerability severity that causes the security stage to fail. Vulnerabilities below this level are reported but do not block the pipeline.",
          "type": "string",
          "enum": ["critical", "high", "medium", "low"]
        }
      }
    },
    "package": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "docker": {
          "description": "Docker image build and push configuration.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "image": {
              "description": "Full image name including registry and repository (e.g. registry.example.com/my-service).",
              "type": "string",
              "minLength": 1
            },
            "dockerfile": {
              "description": "Path to the Dockerfile relative to the build context. Default is Dockerfile.",
              "type": "string",
              "minLength": 1
            },
            "context": {
              "description": "Docker build context path. Default is . (current directory).",
              "type": "string",
              "minLength": 1
            },
            "platforms": {
              "description": "Target platforms for multi-arch builds (e.g. linux/amd64, linux/arm64).",
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              },
              "minItems": 1,
              "uniqueItems": true
            },
            "build_args": {
              "description": "Docker build arguments passed as --build-arg. Keys and values must be strings.",
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            }
          }
        }
      }
    },
    "publish": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "npm": {
          "description": "Publish to npm registry.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "registry": {
              "description": "npm registry URL. Default is https://registry.npmjs.org.",
              "type": "string",
              "format": "uri"
            },
            "tag": {
              "description": "Distribution tag for the published package. Default is latest.",
              "type": "string",
              "default": "latest"
            },
            "access": {
              "description": "Package access level for scoped packages.",
              "type": "string",
              "enum": ["public", "restricted"]
            },
            "token_var": {
              "description": "Name of the environment variable holding the npm auth token. Never put the token value here.",
              "type": "string",
              "minLength": 1
            }
          }
        },
        "docker": {
          "description": "Push Docker image to a container registry.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "image": {
              "description": "Full image name including registry and repository (e.g. ghcr.io/org/app).",
              "type": "string",
              "minLength": 1
            },
            "registry": {
              "description": "Container registry URL for docker login.",
              "type": "string",
              "minLength": 1
            },
            "tags": {
              "description": "List of tags to push. If empty, uses BRIK_VERSION.",
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              },
              "minItems": 1,
              "uniqueItems": true
            },
            "username_var": {
              "description": "Name of the environment variable holding the registry username.",
              "type": "string",
              "minLength": 1
            },
            "password_var": {
              "description": "Name of the environment variable holding the registry password or token.",
              "type": "string",
              "minLength": 1
            }
          }
        },
        "maven": {
          "description": "Publish to a Maven repository.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "repository": {
              "description": "Maven repository URL.",
              "type": "string",
              "format": "uri"
            },
            "username_var": {
              "description": "Name of the environment variable holding the repository username.",
              "type": "string",
              "minLength": 1
            },
            "password_var": {
              "description": "Name of the environment variable holding the repository password.",
              "type": "string",
              "minLength": 1
            }
          }
        },
        "pypi": {
          "description": "Publish to PyPI or a compatible registry.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "repository": {
              "description": "PyPI repository URL. Default is https://upload.pypi.org/legacy/.",
              "type": "string",
              "format": "uri"
            },
            "token_var": {
              "description": "Name of the environment variable holding the PyPI API token.",
              "type": "string",
              "minLength": 1
            }
          }
        },
        "cargo": {
          "description": "Publish to crates.io or a compatible registry.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "registry": {
              "description": "Cargo registry name. Default is crates-io.",
              "type": "string"
            },
            "token_var": {
              "description": "Name of the environment variable holding the crates.io API token.",
              "type": "string",
              "minLength": 1
            }
          }
        },
        "nuget": {
          "description": "Publish to NuGet or a compatible feed.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "source": {
              "description": "NuGet source URL. Default is https://api.nuget.org/v3/index.json.",
              "type": "string",
              "format": "uri"
            },
            "api_key_var": {
              "description": "Name of the environment variable holding the NuGet API key.",
              "type": "string",
              "minLength": 1
            }
          }
        }
      }
    },
    "deploy": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "environments": {
          "description": "Named deployment environments. Each key is an environment name (e.g. staging, production). Each value defines the deployment condition and target.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/deployEnvironment"
          },
          "minProperties": 1
        }
      }
    },
    "deployEnvironment": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "when": {
          "description": "Condition expression evaluated by the shared library to decide whether this environment is deployed. Supports branch equality (branch == 'main'), glob matching (tag =~ 'v*'), and native CI variables ($CI_PIPELINE_SOURCE == 'merge_request_event').",
          "type": "string",
          "minLength": 1
        },
        "target": {
          "description": "Deployment target type.",
          "type": "string",
          "enum": ["ssh", "compose", "k8s", "helm", "gitops"]
        },
        "namespace": {
          "description": "Kubernetes namespace for k8s and helm targets.",
          "type": "string",
          "minLength": 1
        },
        "manifest": {
          "description": "Path to the Kubernetes manifest file or directory for the k8s target.",
          "type": "string",
          "minLength": 1
        },
        "repo": {
          "description": "GitOps infrastructure repository reference (e.g. org/repo-name) for the gitops target.",
          "type": "string",
          "minLength": 1
        },
        "path": {
          "description": "Path within the GitOps repository where manifests for this service are located.",
          "type": "string",
          "minLength": 1
        },
        "controller": {
          "description": "GitOps controller responsible for reconciliation (e.g. argocd, fluxcd).",
          "type": "string",
          "minLength": 1
        },
        "app_name": {
          "description": "Application name as known to the GitOps controller.",
          "type": "string",
          "minLength": 1
        }
      }
    },
    "notifyEventList": {
      "description": "Pipeline events that trigger a notification.",
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["failure", "success", "always"]
      },
      "minItems": 1,
      "uniqueItems": true
    },
    "notify": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "slack": {
          "description": "Slack notification configuration.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "channel": {
              "description": "Slack channel name including the # prefix (e.g. #deployments).",
              "type": "string",
              "minLength": 1
            },
            "on": {
              "description": "Pipeline events that trigger a Slack notification.",
              "$ref": "#/$defs/notifyEventList"
            }
          }
        },
        "email": {
          "description": "Email notification configuration.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "to": {
              "description": "Recipient email address or comma-separated list of addresses.",
              "type": "string",
              "minLength": 1
            },
            "on": {
              "description": "Pipeline events that trigger an email notification.",
              "$ref": "#/$defs/notifyEventList"
            }
          }
        },
        "webhook": {
          "description": "Webhook notification configuration.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "url": {
              "description": "Webhook endpoint URL.",
              "type": "string",
              "format": "uri",
              "minLength": 1
            },
            "on": {
              "description": "Pipeline events that trigger a webhook call.",
              "$ref": "#/$defs/notifyEventList"
            }
          }
        }
      }
    },
    "hookCommandList": {
      "description": "Ordered list of shell commands to execute at a hook point.",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1
    },
    "hooks": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "pre_init": {
          "description": "Commands executed before the init stage.",
          "$ref": "#/$defs/hookCommandList"
        },
        "post_init": {
          "description": "Commands executed after the init stage completes successfully.",
          "$ref": "#/$defs/hookCommandList"
        },
        "pre_release": {
          "description": "Commands executed before the release stage.",
          "$ref": "#/$defs/hookCommandList"
        },
        "post_release": {
          "description": "Commands executed after the release stage completes successfully.",
          "$ref": "#/$defs/hookCommandList"
        },
        "pre_build": {
          "description": "Commands executed before the build stage.",
          "$ref": "#/$defs/hookCommandList"
        },
        "post_build": {
          "description": "Commands executed after the build stage completes successfully.",
          "$ref": "#/$defs/hookCommandList"
        },
        "pre_quality": {
          "description": "Commands executed before the quality stage.",
          "$ref": "#/$defs/hookCommandList"
        },
        "post_quality": {
          "description": "Commands executed after the quality stage completes successfully.",
          "$ref": "#/$defs/hookCommandList"
        },
        "pre_security": {
          "description": "Commands executed before the security stage.",
          "$ref": "#/$defs/hookCommandList"
        },
        "post_security": {
          "description": "Commands executed after the security stage completes successfully.",
          "$ref": "#/$defs/hookCommandList"
        },
        "pre_test": {
          "description": "Commands executed before the test stage.",
          "$ref": "#/$defs/hookCommandList"
        },
        "post_test": {
          "description": "Commands executed after the test stage completes successfully.",
          "$ref": "#/$defs/hookCommandList"
        },
        "pre_package": {
          "description": "Commands executed before the package stage.",
          "$ref": "#/$defs/hookCommandList"
        },
        "post_package": {
          "description": "Commands executed after the package stage completes successfully.",
          "$ref": "#/$defs/hookCommandList"
        },
        "pre_deploy": {
          "description": "Commands executed before the deploy stage.",
          "$ref": "#/$defs/hookCommandList"
        },
        "post_deploy": {
          "description": "Commands executed after the deploy stage completes successfully.",
          "$ref": "#/$defs/hookCommandList"
        },
        "pre_notify": {
          "description": "Commands executed before the notify stage.",
          "$ref": "#/$defs/hookCommandList"
        },
        "post_notify": {
          "description": "Commands executed after the notify stage completes successfully.",
          "$ref": "#/$defs/hookCommandList"
        }
      }
    }
  }
}
